<views:Dialog x:Class="Twice.Views.Wizards.AccountColumnsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:Twice.Views"
			  DataContext="{Binding AccountColumns, Source={StaticResource DialogLocator}}"
             xmlns:lex="http://wpflocalizeextension.codeplex.com" mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition />
		</Grid.RowDefinitions>

		<TextBlock Text="{lex:Loc SelectColumnsYouWantToAddForNewAccount}" Grid.Row="0" />

		<CheckBox Content="{lex:Loc SelectAll}" IsChecked="{Binding SelectAll, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" />

		<ItemsControl Grid.Row="2" ItemsSource="{Binding AvailableColumnTypes}">
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
							  Content="{Binding Content.DisplayName}" ToolTip="{Binding Content.Description}" />
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
	</Grid>
</views:Dialog>